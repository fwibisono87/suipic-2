# Sprint 1: Foundation & Auth

## Goal
Establish the core technical stack, database schema, and authentication flow.

## Tasks
- [x] Initialize project structure (apps/packages/infra)
- [x] Create root docker-compose.yml and .env.example
- [x] Initialize Bun project and install dependencies (SvelteKit, ElysiaJS, TailwindCSS, DaisyUI).
- [ ] Set up Docker Compose for PostgreSQL, MinIO, and Keycloak.
- [ ] Configure Keycloak with initial realm and roles (`admin`, `photographer`, `client`).
- [ ] Implement backend authentication middleware (JWT validation).
- [ ] Implement seeded admin bootstrap.
- [ ] Basic frontend routing and layout.
